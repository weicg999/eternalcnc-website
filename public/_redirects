# Cloudflare Pages _redirects
# ---------------------------------------------------------------------------
# 语法限制（官方文档 pages/configuration/redirects）：
#   * source 只能是「路径」，**不支持域名级跳转**（裸域 <-> www 在本文件里做不到），
#     必须在 Cloudflare 后台 Rules -> Redirect Rules / Bulk Redirects（或遗留 Page Rules）配。
#   * 带斜杠与不带斜杠是**两条不同的规则**（官方示例 `/trailing /trailing/ 301`），
#     所以 /about-us 与 /about-us/ 必须各写一行，否则带斜杠的那个会 404。
#   * 本文件的规则**不作用于 Pages Functions 的请求**（本仓库有 functions/chat-token.js，路径不重叠）。
#
# 域名规范现状（2026-09-21 定稿）
#   全站规范域 = https://eternalcnc.com（裸域）；Cloudflare 侧为【www -> 301 -> 裸域】，两者一致。
#   规范域信号来源（已全部统一为裸域）：
#     astro.config.mjs 的 site / 页面 canonical / og:url / hreflang /
#     sitemap 与 sitemap-index / robots.txt 的 Sitemap 声明 /
#     JSON-LD 中的 url | logo | image /
#     scripts/indexnow-push.mjs 的 HOST / baidu_push.py 的 SITE / baidu-push-urls.txt
#   ⚠️ 不要再把 Cloudflare 的跳转规则改成【裸域 -> www】——那会把方向反过来，
#      重新制造「canonical 与服务器行为打架」的闭环矛盾。
#   ⚠️ functions/ 的 CORS 白名单同时保留裸域与 www（防御性），不要只留一个。
# ---------------------------------------------------------------------------

# SEO: 旧路径 /about-us -> /about/（带斜杠变体必须单独写一行）
/about-us      /about/      301
/about-us/     /about/      301
/zh/about-us   /zh/about/   301
/zh/about-us/  /zh/about/   301
